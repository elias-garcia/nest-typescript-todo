import 'reflect-metadata';
import { NestContainer } from './injector/container';
import { NestModuleMetatype } from '../common/interfaces/module-metatype.interface';
export declare class DependenciesScanner {
    private container;
    constructor(container: NestContainer);
    scan(module: NestModuleMetatype): void;
    private scanForModules(module);
    private storeModule(module);
    private scanModulesForDependencies();
    private reflectRelatedModules(module);
    private reflectComponents(module);
    private reflectControllers(module);
    private reflectExports(module);
    private reflectExceptionFilters(component, module);
    private reflectGatewaysMiddlewares(component, module);
    private storeRelatedModule(related, module);
    private storeComponent(component, module);
    private storeExportedComponent(exportedComponent, module);
    private storeRoute(route, module);
    private reflectMetadata(module, metadata);
}
