import 'reflect-metadata';
export interface Constructor<T> {
    new (...args: any[]): T;
}
export declare const ProvideValues: <T extends Constructor<{}>>(data: {
    [param: string]: any;
}) => (metatype: T) => any;
