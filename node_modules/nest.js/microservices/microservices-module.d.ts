import { NestContainer, InstanceWrapper } from '../core/injector/container';
import { Controller } from '../common/interfaces/controller.interface';
export declare class MicroservicesModule {
    private static readonly listenersController;
    static setupListeners(container: NestContainer, server: any): void;
    static setupClients(container: NestContainer): void;
    static bindListeners(controllers: Map<string, InstanceWrapper<Controller>>, server: any): void;
    static bindClients(controllers: Map<string, InstanceWrapper<Controller>>): void;
}
