export declare abstract class Server {
    private readonly logger;
    protected readonly msgHandlers: {};
    abstract listen(callback: () => void): any;
    getHandlers(): {};
    add(pattern: any, callback: any): void;
    protected handleError(error: any): void;
}
