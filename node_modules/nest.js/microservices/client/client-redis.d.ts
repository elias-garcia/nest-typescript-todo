import { ClientProxy } from './client-proxy';
import { ClientMetadata } from '../interfaces/client-metadata.interface';
export declare class ClientRedis extends ClientProxy {
    private readonly logger;
    private readonly DEFAULT_URL;
    private readonly url;
    private pub;
    private sub;
    constructor({url}: ClientMetadata);
    sendSingleMessage(msg: any, callback: (...args) => any): (channel: any, message: any) => void;
    getAckPatternName(pattern: string): string;
    getResPatternName(pattern: string): string;
    private init();
    private createClient();
    private handleErrors(stream);
}
