import { ClientProxy } from './client-proxy';
import { ClientMetadata } from '../interfaces/client-metadata.interface';
export declare class ClientTCP extends ClientProxy {
    private readonly DEFAULT_HOST;
    private readonly DEFAULT_PORT;
    private readonly port;
    private readonly host;
    constructor({port, host}: ClientMetadata);
    sendSingleMessage(msg: any, callback: (...args) => any): void;
    createCallback(callback: (...args) => any): (err: any, res: any) => void;
}
