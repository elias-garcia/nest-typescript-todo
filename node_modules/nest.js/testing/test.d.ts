import { ModuleMetadata } from '../common/interfaces/module-metadata.interface';
import { Metatype } from '../common/interfaces/metatype.interface';
export declare class Test {
    private static container;
    private static scanner;
    private static instanceLoader;
    static createTestingModule(metadata: ModuleMetadata): void;
    static get<T>(metatype: Metatype<T>): T;
    static restart(): void;
    private static init();
    private static findInstanceByPrototype<T>(metatype, modules);
    private static createModule(metadata);
}
