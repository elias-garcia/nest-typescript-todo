import 'reflect-metadata';
import { NestContainer, InstanceWrapper } from '../core/injector/container';
import { Injectable } from '../common/interfaces/injectable.interface';
export declare class SocketModule {
    private static socketsContainer;
    private static webSocketsController;
    static setup(container: NestContainer): void;
    static hookGatewaysIntoServers(components: Map<string, InstanceWrapper<Injectable>>, moduleName: string): void;
}
